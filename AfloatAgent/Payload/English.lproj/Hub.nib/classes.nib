{
    IBClasses = (
        {
            ACTIONS = {
                lessTransparent = id; 
                makeMediumTransparency = id; 
                makeOpaque = id; 
                moreTransparent = id; 
                performSeethru = id; 
                resetAllOverlays = id; 
                showAdjustEffectsPanel = id; 
                sinkFocusedWindow = id; 
                toggleAlwaysOnTop = id; 
                toggleKeepAfloat = id; 
            }; 
            CLASS = AfloatHub; 
            LANGUAGE = ObjC; 
            OUTLETS = {adjustEffectsPanel = NSPanel; menuWithModelItems = NSMenu; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}